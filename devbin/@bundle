#!/usr/bin/env __run

fout="$(mktemp)"
>&2 swipl -l compile.pl -- "$1" $fout

cat runtime.js $fout
echo
# Hack: Suppress nonzero exit codes to make test harness simpler.
echo "
try {
	main_0().next();
} catch(e) {
	console.error(e);
}"
