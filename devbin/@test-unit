#!/usr/bin/env __run
test_cmd=run_tests
if [[ -v 1 ]]; then
	test_cmd="run_tests($1)"
fi

testfiles=$(find ./lib/ -name '*_test.pl' | sed 's/^/-l /')

swipl              \
  $testfiles       \
	-g "$test_cmd"   \
	-g halt
